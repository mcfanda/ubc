% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unbiased_estimates.R
\name{ubc_lm}
\alias{ubc_lm}
\title{Estimate bias corrected coefficients of a GLM}
\usage{
ubc_lm(
  formula,
  candidates,
  stage2,
  varnames = NULL,
  index = 2,
  method = "WLS",
  loo = TRUE,
  use.losers = TRUE,
  standardize = TRUE,
  se_method = c("posterior", "calibration", "full", "none")
)
}
\arguments{
\item{formula}{A formula of the form y~. or y~x+. in which the dot \code{.} is replaced
by each input variable. A GLM is estimated for each \code{varnames} variable by replace it in the formula}

\item{candidates}{Discovery (stage 1) data with candidates variables (winners and controlling loosers).}

\item{stage2}{A data.frame with stage 2 (confirmatory) data}

\item{index}{the index of the coefficient in the model results to analyze. Default is 2, indicating the first effect in a GLM (1 would be the intercept)}

\item{method}{\code{"WLS"} (default) weights the estimation with 1/SE^2, where SE is the Stage 2 estimates standard errors.
\code{"OLS"} does not apply any weights.}

\item{loo}{apply the leave-one-out algorithm to estimate the unbiased coefficients}

\item{standardize}{Whether to standardize the variables before estimation. Default \code{TRUE}}
}
\value{
A data.frame with the unbiased estimates, SE, t-test and p-values of the selected variables
}
\description{
This function estimates bias corrected coefficients of a general model based on the UBC method.
Coefficients are computed considering both the discovery data and the stage 2 data.
}
