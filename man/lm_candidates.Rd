% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unbiased_estimates.R
\name{lm_candidates}
\alias{lm_candidates}
\title{Find the variables candidated for selections (winners)}
\usage{
lm_candidates(
  formula,
  data,
  varnames = NULL,
  index = 2,
  sig.level = 0.1,
  prop.loser = 0.33
)
}
\arguments{
\item{formula}{A formula of the form y~. or y~x+. in which the dot \code{.} is replaced
by each input variable. A GLM is estimated for each \code{varnames} variable by replace it in the formula}

\item{data}{A data.frame containing the variables to analyze}

\item{varnames}{a vector of variables names. If NULL, all variables in the dataframe are tested, excluding the dependent variable}

\item{index}{the index of the coefficient in the model results to analyze. Default is 2, indicating the first effect in a GLM (1 would be the intercept)}

\item{sig.level}{The critical alpha level to be used in the selection of the variable, default .10}

\item{prop.loser}{Proportion of control variables (loosers) to select with respect of the selected ones (winners). Default .33 indicates selecting 1 looser for 3 winners}
}
\value{
A table with the estimates, SE, t-test and p-values of the selected variables
}
\description{
This function estimates a general model parameters for a set
of variables and identifies the plausible candidates for selection (winners) and
a set of control variables (loosers) based on significance level
}
